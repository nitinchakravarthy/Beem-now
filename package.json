{
  "name": "BEEM_APP",
  "version": "0.0.1",
  "description": "BEEM NOW APP - full stack environment",
  "main": "index.js",
  "scripts": {
    "clientbuild": "cd client && npm install && npm bablebuild && npm run build"
    "client": "cd client && npm install && npm bablebuild && npm start",
    "server": "cd server && npm install && npm start",
    "start": "concurrently --kill-others \"npm run server\" \"npm run client\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@material-ui/lab": "^4.0.0-alpha.33",
    "concurrently": "^5.0.0",
    "moment": "^2.24.0",
    "moment-timezone": "^0.5.27"
  }
}
